import React from 'react';

// needs to be a redux container to keep state of the sign in input to show favorties when signed in or "sign in" when not.
const Header = () => {
  return (
    <div>
      <h1>Movie Tracker, YO!</h1>
      {/* <navLink path={/}></navLink> */}
    </div>
  );
};

export default Header;
